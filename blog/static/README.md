# Static Assets

Place your custom static files here:

- `album-cover.jpg` ? background image for the blog (recommended: 1920?1080 or larger)
- `favicon.ico` ? site favicon
- `og-image.png` ? Open Graph image for social sharing (1200?630 recommended)
